name: Social Monitor

on:
  schedule:
    # KST 08:00-21:00 hourly
    - cron: "0 23 * * *"
    - cron: "0 0-12 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Run mode: check sends new posts, seed stores current latest posts only"
        required: true
        default: "check"
        type: choice
        options:
          - check
          - seed

permissions:
  contents: write

concurrency:
  group: social-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run monitor
        env:
          DISCORD_WEBHOOK_INSTAGRAM_AI_FREAKS: ${{ secrets.DISCORD_WEBHOOK_INSTAGRAM_AI_FREAKS }}
          DISCORD_WEBHOOK_INSTAGRAM_PROMPPY: ${{ secrets.DISCORD_WEBHOOK_INSTAGRAM_PROMPPY }}
          DISCORD_WEBHOOK_INSTAGRAM_AI_AINOW: ${{ secrets.DISCORD_WEBHOOK_INSTAGRAM_AI_AINOW }}
          DISCORD_WEBHOOK_THREADS_CHOI_OPENAI: ${{ secrets.DISCORD_WEBHOOK_THREADS_CHOI_OPENAI }}
          DISCORD_WEBHOOK_YOZM_AI: ${{ secrets.DISCORD_WEBHOOK_YOZM_AI }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_TAG_MODEL: ${{ vars.ANTHROPIC_TAG_MODEL || 'claude-haiku-4-5-20251001' }}
          DISCORD_TAG_AI_CODING: ${{ secrets.DISCORD_TAG_AI_CODING }}
          DISCORD_TAG_PROMPT_TIPS: ${{ secrets.DISCORD_TAG_PROMPT_TIPS }}
          DISCORD_TAG_AGENT_AUTOMATION: ${{ secrets.DISCORD_TAG_AGENT_AUTOMATION }}
          DISCORD_TAG_MODEL_SERVICE: ${{ secrets.DISCORD_TAG_MODEL_SERVICE }}
          DISCORD_TAG_BUSINESS_INVESTMENT: ${{ secrets.DISCORD_TAG_BUSINESS_INVESTMENT }}
          DISCORD_TAG_INFRA_SEMICONDUCTOR: ${{ secrets.DISCORD_TAG_INFRA_SEMICONDUCTOR }}
          DISCORD_TAG_POLICY_REGULATION: ${{ secrets.DISCORD_TAG_POLICY_REGULATION }}
          DISCORD_TAG_SECURITY_SAFETY: ${{ secrets.DISCORD_TAG_SECURITY_SAFETY }}
          DISCORD_TAG_USE_CASE: ${{ secrets.DISCORD_TAG_USE_CASE }}
        run: npm run ${{ github.event.inputs.mode || 'check' }}

      - name: Commit seen state
        run: |
          if git diff --quiet -- seen.json; then
            echo "seen.json unchanged"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add seen.json
          git commit -m "chore: update social monitor seen state"
          git push
